#include "board.h"

/*------------------------------------------------------------------------------
 * Trip-zone configuration (TZn inputs, one-shot / cycle-by-cycle actions).
 * Currently a placeholder.
 *------------------------------------------------------------------------------*/
void Board_initTripZone(void)
{
}
